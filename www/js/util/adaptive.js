define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone){
    var Adaptive = {
        adaptiveClass: function(resolution) {
            if(resolution < 400 && resolution >= 340){

            }
        }
    };

    return Adaptive;
});